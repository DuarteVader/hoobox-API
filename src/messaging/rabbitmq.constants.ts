export const ORDERS_EXCHANGE = 'orders.exchange';

export const ORDER_CREATED_QUEUE = 'orders.created.queue';

export const ORDER_CREATED_ROUTING_KEY = 'order.created';

export const ORDERS_RETRY_EXCHANGE = 'orders.retry.exchange';

export const ORDER_RETRY_QUEUE = 'orders.retry.queue';

export const ORDER_RETRY_ROUTING_KEY = 'order.retry';

export const ORDERS_DLX = 'orders.dlx';

export const ORDER_DLQ = 'orders.dlq';

export const ORDER_FAILED_ROUTING_KEY = 'order.failed';

export const RETRY_DELAY_MS = 2000;

export const MAX_RETRIES = 3;
